# ===========================================================
#  PT_JP 日本节点 — 环境变量模板
#
#  使用方法:
#    cp .env.example .env
#    vim .env          # 填入真实密码和 Passkey
#
#  ⚠️ .env 包含敏感信息，已被 .gitignore 排除，绝不入库！
# ===========================================================

# ---- 镜像版本 (锁定！防止自动升级到白名单外) ----
# 查看可用tag: https://hub.docker.com/r/linuxserver/qbittorrent/tags
# 务必确认为 Official 版本，非 Enhanced Edition
QB_IMAGE_TAG=4.6.7

# ---- 端口 ----
QB_WEBUI_PORT=8080
QB_BT_PORT=6881

# ---- WebUI 认证 (deploy.sh 首次启动后修改密码，然后填入此处) ----
# disk_guard.sh 会读取此密码用于紧急暂停API调用
QB_USER=admin
QB_PASS=CHANGE_ME_AFTER_FIRST_LOGIN

# ---- 系统 ----
PUID=1000
PGID=1000
TZ=Asia/Shanghai

# ---- 资源限制 ----
MEM_LIMIT=4g
MEM_SWAP=5g
CPU_LIMIT=3.5

# ---- MT站 RSS (敏感！绝不入库) ----
# 获取路径: MT站 → 控制面板 → RSS订阅
# spstate=2 = 仅显示 Free 种子 (第一层过滤，在URL层面)
MT_RSS_URL=https://kp.m-team.cc/api/rss/dl?passkey=YOUR_PASSKEY_HERE&https=1&spstate=2